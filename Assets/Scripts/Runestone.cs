using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Runestone : MonoBehaviour
{/*
    // Runestone reveals its runes/secrets
    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.gameObject.layer == LayerMask.NameToLayer("Player"))
        {
            if (coins == 3)  //  && open == falseIf 3 coins and First time the runestone is revealing its runes
            { 
                //RunestoneReveal runestoneReveal = collision.GetComponent<RunestoneReveal>(); // Script RunestoneReveal
                //runestoneReveal.Reveal(); // Do not know why not working! Same as coin!
                FindObjectOfType<RunestoneReveal>().Reveal();// Script RunestoneReveal, funktion Reveal
            }
        }
    }*/
}
